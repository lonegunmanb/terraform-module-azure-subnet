name: E2E Test
on:
  pull_request:
    types: [ 'opened', 'synchronize' ]
    paths:
      - '.github/**'
      - '**.go'
      - '**.tf'
      - '.github/workflows/**'
      - '**.md'

jobs:
  acc-tests:
    runs-on: self-hosted
    environment:
      name: acctests
    steps:
      - uses: actions/checkout@v3
      - run: |
          export ARM_SUBSCRIPTION_ID=$(curl -s -H Metadata:true "http://169.254.169.254/metadata/instance?api-version=2017-08-01" | jq -r '.compute | .subscriptionId')
          export ARM_TENANT_ID=$(az login --identity | jq -r '.[0] | .tenantId')
          docker run --rm --runtime=runc -e ARM_SUBSCRIPTION_ID -e ARM_TENANT_ID -e ARM_USE_MSI=true -v $(pwd):/src -w /src testrunner.azurecr.io/testrunner/tfmodule-testrunner:latest sh -c "PATH=$PATH:/go/bin:/usr/local/go/bin make e2e-test"